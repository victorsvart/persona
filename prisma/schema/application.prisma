model Application {
    id           String @id @default(uuid())
    company_name String
    role         String
    details      String
    userId       String
    user         User   @relation(fields: [userId], references: [id], onDelete: Cascade)

    @@map("applications")
    @@schema("public")
}
